# name: RandomEdits
# version: 1.0.5
# CubKit build info:
# CubKit source sha256: f7b22fee2ed390235b0d7d42864a114b7abc734889282d435c004cd5dd9caf2e
# CubKit payload sha256: e8af922bbd9a3cd98bae4fb1b4f0b13d91d5b8318701ce2dd601343102cf6db3
# CubKit source map:
# - generated line 118 -> RandomEdits.py:1
# - bundled files are extracted from the CubKit payload at import time:
#   - randomedits_lib/__init__.py -> __init__.py:1 (lines: 9, sha256: 4296e3efb6cf0ac938903356dba7190faff3ea64676d36f74f438eb3b0dd7353)
#   - randomedits_lib/strings_random_media.py -> strings_random_media.py:1 (lines: 55, sha256: ab3ed15a2ef24c100131ad4112c2aa71cc13c787b7af718d2bd0838d5d02f9ce)
#   - randomedits_lib/utils_mixin.py -> utils_mixin.py:1 (lines: 68, sha256: 1e566b1331999637b558d0f8957571e104393ffbdbf4ee9247e05603d41f9cad)

from __future__ import annotations
# Generated by CubKit. Do not edit this header by hand.
# CubKit repository: https://github.com/hairpin01/CubKit
# CubKit build notes:
# - Metadata comments above were generated/normalized from cubkit.toml and entrypoint code.
# - Bundled helper files are stored below as a base85-encoded zip payload.
# - On import, CubKit verifies the payload SHA256 and extracts it into CUBKIT_CACHE_DIR or ~/.cache/cubkit.
# - CubKit import-debug comments below explain sys.path/package wiring for private relative imports.
__cubkit_module_id__ = 'randomedits'
__cubkit_package_dirs__ = ('randomedits_lib',)
__cubkit_bundle_sha256__ = 'e8af922bbd9a3cd98bae4fb1b4f0b13d91d5b8318701ce2dd601343102cf6db3'
__cubkit_bundle_b85__ = """
P)h>@6aWAK2mk;8ApiuY0JVky006B3000{R0044fZe(w5Wn^h|b6;#}VlQ7`X>MtBUtcb8c}<J43c^4TMEm=SWt%jTKd{k4EFxl(
N@Q8gB`)6Hg}aOR`6d{_>E6s6L_9}%aken>{mFxcK19e5Nl@+E9cbt#XG=shrjJ3POgV}l5=Hi1P*U3ogXkDyhPt^NPltLnrz|d!
%3J(TplYFZ8H^!f=ZIhnR|9j3^<Heg_$SZR&3ph*O9KQH000080000X0BuiM5}yPB0O1V)04D$d0CHh&WN&R{WNCDBUu<b&FLQKq
X>Mn8Uvgn?WN&R>ZDnL>VJ>ia#aG>K8b=hq=P8c3!Iu033%d(0;ii?^Tr?N8bJbL6*&Um$ml?Xdm{^u2+bBw+IJK%;YHQVvBK4wI
6`>G=ZRjh^zCvE0eT1HwUBC_|ah1dYv<$On=X~d!?>k4tV#_OE-
M;hrGTef#7~m`J*5XUiTD%hLWV7fQm8wIEj$N_+#1bU<bAHMP{4CfP-
@yOogJ74R@eXhEKJNw(0Dlhf{UUgbo4Cb$fTm4cHqow$j|4mNp>y60_Hf<hExFncZ*_sUf+ze1+U)R7V(u~?vusdG90}!5e!@Fw)
JI1>em)1cKLb4N437wQ@%;psr@~`jTt*n?5$0~Nhvz?(_)Z~sis-vyi??6~gKGp2gGan0p6q}GIpBYVAZITiidSuO9dnk*`)l*0!
oj4%p!(M<%iF-k#$yf+SA05X85q{A&Cxw{J?MXestuxgBM<zbsAW4;@a1&tCflt8=-
i80M9Sl?r#Bzac{KpvxtbbdVDfpg*jt{$7CRNGy4jfi7;{yKN?<lU(t5iB246*C}X;c8}6vLn!EgE`Ul3V873yD@wz0GdW1$&I=p
nTW=(0}1Ab(Qr=!hU)7R%p3@3q$!Jes?`6G>yeUE8?=eb1bZo_$9<iD?P)-7CHfxp-lr_HTk#D+3&A-
$!sFHl(WT`jx{=F$Sl^10^y<RV)WjmPKcP>8A+}5M#1m1leKAH=exXyuM65y1bEVx#eS|4N6wGr7(k$PEa!|!A9%FrQMXpUuluu^
&hOol^7At~#EYpbI3xNCs7e3%VoBm}Yd?M@Ku?dISbeiCqPx5f%0v)YXP~5wdJSEk_;lnqf$kLal+@#NCz(DP0cv*(@!aCrc*~5k
|+fL>4kjaHYynJNNg!BhS9Y_ls_J4-C-JGBH1d-
Ln?hxe2>E{GbF0Bo`(Qs*L{0`TC9m0x`sh*1BC|poaI6?C9x$=^JsP{CaQsRXq5PU1|Deo>qM0boTe93A#VtG5$gchOQJ6zj%m>}
bvx}rZ6bj62kPm_NGPuVW}3s6e|0u%!j000080000X00gH1wT1uy0IdK302=@R00000000000Hgr`0001TVQyq^ZDnL>baP*9X<{
#5UukY>bYEXCaCuNm0Rj{Q6aWAK2mk;8ApmVpSrVTF007|)001Wd0000000000005)`zW@LLa$#;{Z*65{X>@a6Y-
wUIb98cPZfA2}a$#;{Z*5;~Wn^h#E^v8JO928D0~7!N00;m803iTun*$8y0ssJc2mk;c00000000000001_0iy)~0CHh&WN&R{WN
CDBUu<b&FLiWjY;#|2X?SUFE^v8JO9ci10000300RK(0002R2mk;800
"""

def __cubkit_bootstrap__():
    # CubKit import-debug: this function is generated by CubKit.
    # It prepares bundled files before the real MCUB module code below runs.
    import base64
    import hashlib
    import os
    import sys
    import zipfile
    from pathlib import Path

    # CubKit import-debug: decode the embedded base85 zip payload.
    data = base64.b85decode("".join(__cubkit_bundle_b85__.split()).encode("ascii"))
    digest = hashlib.sha256(data).hexdigest()
    # CubKit import-debug: fail fast if the embedded payload was corrupted.
    if digest != __cubkit_bundle_sha256__:
        raise RuntimeError("CubKit embedded bundle checksum mismatch")

    # CubKit import-debug: cache extraction avoids rewriting helper files on every import.
    cache_root = Path(os.environ.get("CUBKIT_CACHE_DIR", Path.home() / ".cache" / "cubkit"))
    bundle_dir = cache_root / __cubkit_module_id__ / digest
    marker = bundle_dir / ".cubkit-extracted"
    if not marker.exists():
        bundle_dir.mkdir(parents=True, exist_ok=True)
        archive_path = bundle_dir / "bundle.zip"
        archive_path.write_bytes(data)
        with zipfile.ZipFile(archive_path) as archive:
            archive.extractall(bundle_dir)
        marker.write_text(digest, encoding="utf-8")

    # CubKit import-debug: expose extracted top-level files for normal absolute imports.
    bundle_path = str(bundle_dir)
    if bundle_path not in sys.path:
        sys.path.insert(0, bundle_path)

    # CubKit import-debug: build private package search paths for `from .utils import ...`.
    relative_import_paths = [bundle_path]
    for package_dir in reversed(__cubkit_package_dirs__):
        package_path = bundle_dir / package_dir
        if package_path.is_dir():
            relative_import_paths.insert(0, str(package_path))

    # CubKit import-debug: mark the generated main module as package-like.
    # This prevents accidental imports from global MCUB packages named `utils`, `lib`, etc.
    module_globals = globals()
    module_globals["__path__"] = relative_import_paths
    module_globals["__package__"] = module_globals.get("__name__", __cubkit_module_id__)
    module_spec = module_globals.get("__spec__")
    if module_spec is not None:
        module_spec.submodule_search_locations = relative_import_paths

__cubkit_bootstrap__()
del __cubkit_bootstrap__

# ---- CubKit entrypoint: RandomEdits.py ----
# scop: kernel min v1.4.5

import html

from typing import Any

from telethon import events
from telethon.errors import (
    ChannelInvalidError,
    ChannelPrivateError,
    FloodWaitError,
    RPCError,
)

from core.lib.loader.module_base import ModuleBase, command
from core.lib.loader.module_config import ConfigValue, EntityLike, Integer, ModuleConfig, Row
from utils import Strings

# Local imports
from randomedits_lib import STRINGS
from randomedits_lib import _MixinUtils

class RandomEdits(
    _MixinUtils,
    ModuleBase
    ):

    name = "RandomEdits"
    version = "1.0.5"
    author = "@modulesanhedonuya && porting by @Hairpin00"
    description = {
        "ru": "Отправляет случайный эдит",
        "en": "Sends a random edit",
    }

    strings: Strings = STRINGS

    config = ModuleConfig(
        ConfigValue(
            "channel",
            "randomeditsforme",
            description=lambda mod: mod.strings('key_channel'),
            validator=EntityLike(),
        ),
        Row(),
        ConfigValue(
            "sample_limit",
            500,
            description=lambda mod: mod.strings('key_sample_limit'),
            validator=Integer(min=1, max=500),
        ),
    )

    async def on_load(self) -> None:
        await super().on_load()

    @command(
        "randomedit",
        doc={
            "ru": "отправить случайный эдит",
            "en": "send a random edit",
        },
    )
    async def cmd_randomedit(self, event: events.NewMessage.Event) -> None:
        status = await self._edit_status(event, self.strings("pick"))

        try:
            ok, error_text = await self._send_random_post(
                event.chat_id,
                reply_to=getattr(event, "reply_to_msg_id", None),
            )
            if not ok:
                await self._edit_status(status, error_text or self.strings("no_posts"))
                return

            await self._edit_status(status, self.strings("done"))
        except (ChannelPrivateError, ChannelInvalidError, ValueError) as exc:
            self.log.warning("RandomEdits source channel is unavailable: %s", exc)
            await self._edit_status(status, self.strings("bad_channel"))
        except FloodWaitError as exc:
            self.log.warning("RandomEdits hit Telegram flood wait: %s", exc)
            await self._edit_status(
                status,
                self.strings("flood").format(seconds=getattr(exc, "seconds", 0)),
            )
        except RPCError as exc:
            self.log.warning("RandomEdits Telegram RPC error: %s", exc)
            error_text = str(exc)
            lowered = error_text.lower()
            if "protected" in lowered or "forbidden" in lowered or "copy" in lowered:
                await self._edit_status(status, self.strings("protected"))
                return
            await self._edit_status(
                status,
                self.strings("rpc_error").format(error=html.escape(error_text)),
            )
        except Exception as exc:
            self.log.exception("Unexpected RandomEdits error")
            await self._edit_status(
                status,
                self.strings("unknown_error").format(error=html.escape(str(exc))),
            )
